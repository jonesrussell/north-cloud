# Place at /etc/enrichment/env, owned root:root, mode 0640.
ENRICHMENT_HOST=0.0.0.0
ENRICHMENT_PORT=8095
ENRICHMENT_READ_TIMEOUT=5s
ENRICHMENT_WRITE_TIMEOUT=10s
ENRICHMENT_SHUTDOWN_TIMEOUT=10s

# The service reads ELASTICSEARCH_URL first, then ES_URL.
ELASTICSEARCH_URL=http://localhost:9200

