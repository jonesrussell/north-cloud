services:
  fetcher:
    image: ghcr.io/jonesrussell/north-cloud-fetcher:latest
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "3"
