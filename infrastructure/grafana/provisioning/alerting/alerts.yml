apiVersion: 1
groups:
  - orgId: 1
    name: "North Cloud Pipeline"
    folder: "North Cloud"
    interval: "1m"
    rules:
      # Alert 1: Pipeline Stall
      - uid: nc-pipeline-stall
        title: "Pipeline stall — no classifications in 2 hours"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 7200
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="classifier"} |= "Classification complete" [2h])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "critical"
          service: "classifier"
        annotations:
          summary: "No content classified in the last 2 hours"
          description: "The classifier has not produced any classification results. Check classifier, Elasticsearch, and raw content availability."

      # Alert 2: Error Spike
      - uid: nc-error-spike
        title: "Error spike — sustained errors across pipeline"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 7200
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'sum by () (rate({service=~"crawler|classifier|publisher", level="error"} [5m]))'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "gt"
                    params:
                      - 0.167
                  operator:
                    type: "and"
        for: "5m"
        labels:
          severity: "warning"
          team: "platform"
        annotations:
          summary: "Elevated error rate in pipeline services"
          description: "More than 10 errors/minute sustained for 5 minutes across pipeline services."

      # Alert 3: Publisher Zero Output
      - uid: nc-publisher-zero
        title: "Publisher zero output — no content published in 4 hours"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 14400
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="publisher"} |= "Published content item to channel" [4h])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "critical"
          service: "publisher"
        annotations:
          summary: "No content published in 4 hours"
          description: "Publisher has not pushed any content to Redis. Check source availability, classifier output, and publisher routing configuration."

      # Alert 4: Deployer Site Silence — Streetcode
      - uid: nc-streetcode-silence
        title: "Deployer site silence — streetcode no logs in 30 minutes"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 1800
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="streetcode"} [30m])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "warning"
          service: "streetcode"
        annotations:
          summary: "streetcode has not produced logs in 30 minutes"
          description: "The Laravel consumer may be down or the log shipping pipeline is broken."

      # Alert 5: Deployer Site Silence — Orewire
      - uid: nc-orewire-silence
        title: "Deployer site silence — orewire no logs in 30 minutes"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 1800
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="orewire"} [30m])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "warning"
          service: "orewire"
        annotations:
          summary: "orewire has not produced logs in 30 minutes"
          description: "The Laravel consumer may be down or the log shipping pipeline is broken."

      # Alert 6: Deployer Site Silence — Coforge
      - uid: nc-coforge-silence
        title: "Deployer site silence — coforge no logs in 30 minutes"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 1800
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="coforge"} [30m])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "warning"
          service: "coforge"
        annotations:
          summary: "coforge has not produced logs in 30 minutes"
          description: "The Laravel consumer may be down or the log shipping pipeline is broken."

      # Alert 7: Deployer Site Silence — Movies of War
      - uid: nc-movies-of-war-silence
        title: "Deployer site silence — movies-of-war no logs in 30 minutes"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 1800
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'count_over_time({service="movies-of-war"} [30m])'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "warning"
          service: "movies-of-war"
        annotations:
          summary: "movies-of-war has not produced logs in 30 minutes"
          description: "The Laravel consumer may be down or the log shipping pipeline is broken."

      # Alert 8: Classification Queue Depth (Prometheus)
      - uid: nc-classifier-queue-depth
        title: "Classification queue depth > 1000"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: "prometheus"
            model:
              expr: "classifier_queue_depth"
              instant: true
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "gt"
                    params:
                      - 1000
                  operator:
                    type: "and"
        for: "5m"
        labels:
          severity: "warning"
          service: "classifier"
        annotations:
          summary: "Classification queue depth exceeds 1000"
          description: "The classifier work queue has more than 1000 pending documents for 5+ minutes. This indicates the classifier cannot keep up with incoming content."

      # Alert 9: Publisher Cursor Lag (Prometheus)
      - uid: nc-publisher-cursor-lag
        title: "Publisher cursor lag > 5 minutes"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: "prometheus"
            model:
              expr: "publisher_cursor_lag_seconds"
              instant: true
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "gt"
                    params:
                      - 300
                  operator:
                    type: "and"
        for: "5m"
        labels:
          severity: "warning"
          service: "publisher"
        annotations:
          summary: "Publisher cursor lag exceeds 5 minutes"
          description: "The publisher is more than 5 minutes behind the latest classified content. Check Elasticsearch query performance and publisher routing throughput."

      # Alert 10: ML Sidecar Error Rate (was Alert 8)
      - uid: nc-ml-sidecar-errors
        title: "ML sidecar error rate — sustained failures"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 3600
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'sum by () (count_over_time({service="classifier"} | json | msg="ML sidecar classification failed" [15m]))'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "gt"
                    params:
                      - 10
                  operator:
                    type: "and"
        for: "5m"
        labels:
          severity: "warning"
          service: "classifier"
        annotations:
          summary: "ML sidecar error rate elevated"
          description: "More than 10 ML sidecar classification failures in 15 minutes. Check sidecar container health and connectivity."

      # Alert 11: ML Sidecars Inactive (was Alert 9)
      - uid: nc-ml-sidecar-inactive
        title: "ML sidecars inactive — no classifications in 2 hours"
        condition: "C"
        noDataState: OK
        execErrState: OK
        data:
          - refId: "A"
            relativeTimeRange:
              from: 7200
              to: 0
            datasourceUid: "loki"
            model:
              expr: 'sum by () (count_over_time({service="classifier"} | json | msg="ML sidecar classification complete" [2h]))'
              queryType: "instant"
          - refId: "C"
            relativeTimeRange:
              from: 0
              to: 0
            datasourceUid: "__expr__"
            model:
              type: "threshold"
              expression: "A"
              conditions:
                - evaluator:
                    type: "lt"
                    params:
                      - 1
                  operator:
                    type: "and"
        for: "0m"
        labels:
          severity: "warning"
          service: "classifier"
        annotations:
          summary: "No ML sidecar classifications in 2 hours"
          description: "No ML sidecars have produced classification results. Check that ML sidecar containers are running and classifier has CRIME_ENABLED/MINING_ENABLED set."
